"""Demonstration recording (v0.5).

This package only *listens*: it samples frames and game state, records the
player's own keyboard/mouse input while the game window is focused, and writes
sessions to disk. Nothing here may send or synthesize input.
"""

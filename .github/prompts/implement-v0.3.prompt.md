Implement the next incomplete task from GitHub Issue #1 for Personal Game AI.

Before editing:
1. Read `AGENTS.md`, `.github/copilot-instructions.md`, `docs/ROADMAP.md`, and the relevant source modules.
2. Identify the smallest coherent v0.3 increment.
3. Preserve all safety invariants, especially explicit input enablement and F8 emergency stop.

Implementation requirements:
- keep vision, state/decision, and input responsibilities separated
- add deterministic tests for new state/rule behavior
- avoid speculative frameworks or large refactors
- update documentation/changelog if behavior is user-visible
- run syntax checks and tests

At the end, summarize:
- files changed
- behavior added
- tests run/results
- remaining limitation
- next recommended Issue #1 checkbox
